{
  "name": "the-last-light",
  "version": "0.2.0",
  "private": true,
  "description": "A cinematic ASCII detective game with branching investigations and quick-time events.",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "start": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "test": "node scripts/build.mjs && node --test tests/*.test.cjs",
    "check": "npm test"
  }
}
